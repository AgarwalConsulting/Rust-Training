# Functions

## Passing arguments

```rust
fn main() {
  add(10, 20);
}

fn add(x: i32, y: i32) {
  println!("{}", x + y);
}
```

## Returning values

```rust
fn five() {
  return 5
}
```

- `return` is optional
